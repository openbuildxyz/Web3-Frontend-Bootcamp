/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: [
        "@ant-design",
        "antd",
        "rc-util",
        "rc-pagination",
        "rc-picker",
    ],
};

export default nextConfig;
