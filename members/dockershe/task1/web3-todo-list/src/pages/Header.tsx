import React from 'react';

const Header = () => {
//   const handleAllFinish = () => {
//     // 处理全部标为完成的逻辑
//   };

  return (
    <div className="bar-message">
      {/* <input type="button" value="全部标为完成" className="btn btn-label btn-allFinish" onClick={handleAllFinish} /> */}
      <div className="bar-message-text">
      Don't try so hard, the best things come when you least expect them to. 不要着急，最好的总会在最不经意的时候出现。 🤏🤏
      </div>
    </div>
  );
};

export default Header;